services:
  - type: web
    name: markdown-to-pdf-api
    runtime: node
    plan: starter
    buildCommand: npm install
    startCommand: npm run start:api
    envVars:
      - key: NODE_VERSION
        value: 20.20.2
      - key: NODE_ENV
        value: production
      - key: ALLOWED_ORIGINS
        sync: false
